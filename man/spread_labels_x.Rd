% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spread_labels.R
\name{spread_labels_x}
\alias{spread_labels_x}
\title{Spread overlapping point labels horizontally}
\usage{
spread_labels_x(
  x,
  labels = NULL,
  min_gap = NULL,
  limits = NULL,
  preserve_center = TRUE,
  padding = grid::unit(1.5, "mm"),
  panel_width = grid::unit(150, "mm"),
  font_size = 3.514598,
  family = wjp_font_family(),
  tol = 1e-08,
  details = FALSE
)
}
\arguments{
\item{x}{Numeric vector of the original point positions, in data units.
\code{NA} positions are returned as \code{NA} and excluded from the solve.}

\item{labels}{Optional character vector (same length as \code{x}) whose rendered
width is used to size the required separation. Requires \code{limits}. Ignored
when \code{min_gap} is supplied. Default is \code{NULL}.}

\item{min_gap}{Optional numeric scalar giving a fixed minimum centre-to-centre
distance between adjacent labels, in data units. Takes precedence over
\code{labels}. Default is \code{NULL}.}

\item{limits}{Optional numeric vector of length 2, \code{c(lower, upper)}, giving
the panel range in data units. The whole label group is kept inside these
limits. Required when \code{labels} is used (to convert text widths to data
units). Default is \code{NULL}.}

\item{preserve_center}{Logical. If \code{TRUE} (default) the adjusted group is kept
centred on the original centroid (minimum squared displacement). If \code{FALSE},
a forward-only pass is used. Default is \code{TRUE}.}

\item{padding}{Extra space added between adjacent label bounding boxes, as a
\link[grid:unit]{grid::unit} (converted to data units via \code{limits}/\code{panel_width}) or a
numeric value in data units. Only used on the \code{labels} path. Default is
\code{grid::unit(1.5, "mm")}.}

\item{panel_width}{Physical width of the plotting panel, as a \link[grid:unit]{grid::unit} or
a numeric value in millimetres. Together with \code{limits} it sets the
data-units-per-millimetre scale used to convert measured text widths. Only
used on the \code{labels} path. Default is \code{grid::unit(150, "mm")}.}

\item{font_size}{Numeric. Label size in the same units as \code{ggplot2}
\code{geom_text(size = )} (millimetres). Only used on the \code{labels} path. Default
is \code{3.514598} (the WJP 10 pt value-label size).}

\item{family}{String. Font family used to measure the labels. Only used on the
\code{labels} path. Default is the active WJP family (see \code{\link[=wjp_font_family]{wjp_font_family()}}).}

\item{tol}{Numeric. Numerical tolerance used when comparing gaps and panel
limits. Default is \code{1e-8}.}

\item{details}{Logical. If \code{TRUE}, returns a list with the adjusted positions
plus diagnostics instead of a bare numeric vector. Default is \code{FALSE}.}
}
\value{
By default, a numeric vector of adjusted horizontal positions in the original
order of \code{x}. If \code{details = TRUE}, a list with:
\describe{
\item{\code{x}}{Adjusted positions (original order).}
\item{\code{input}}{The original \code{x}.}
\item{\code{displacement}}{\code{x} adjusted minus \code{x} input.}
\item{\code{max_displacement}}{Maximum absolute displacement.}
\item{\code{half_width}}{Per-label half-width in data units (original order).}
\item{\code{collided}}{\code{TRUE} if the input positions overlapped.}
\item{\code{feasible}}{\code{FALSE} if the labels cannot fit inside \code{limits}.}
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\code{spread_labels_x()} is a low-level, deterministic helper for row-based point
charts (for example \code{\link[=wjp_dots]{wjp_dots()}} or \code{\link[=wjp_lollipops]{wjp_lollipops()}}). When several
observations in the same row share equal or nearly equal values, the
percentage labels placed above their points overlap and become unreadable.

The helper keeps every point at its true value and every label on the same
vertical level, and moves \strong{only the labels horizontally} by the minimum
distance required to prevent collisions. This avoids the vertical
stair-stepping and connector lines produced by general-purpose repulsion,
yielding a compact result that stays consistent with the visual language of
the chart.
}
\details{
The function returns adjusted horizontal positions for the labels; the points
themselves are never modified. It works within a single row or group at a
time, so it is meant to be called inside a \code{dplyr::group_by()} /
\code{dplyr::mutate()} pipeline (see Examples).

Two ways to size the required separation are supported:
\itemize{
\item \strong{\code{min_gap}} (data units): a fixed minimum centre-to-centre
distance between adjacent labels. This path needs no graphics device and
produces fully reproducible output; it is the simplest option when the
labels are similar in width.
\item \strong{\code{labels}} (text): the rendered width of each string is
measured (with \pkg{systemfonts} when available, otherwise a deterministic
character-count estimate) and converted to data units using \code{limits} and
\code{panel_width}. Use this path when the strings differ substantially in
width.
}

The solver is deterministic. With \code{preserve_center = TRUE} (default) it uses
an isotonic (pool-adjacent-violators) fit that minimises the total squared
displacement, which keeps the centre of the label group on the original
centroid. With \code{preserve_center = FALSE} it uses a single forward pass
anchored at the first label, pushing labels forward only. In both cases,
labels that are already separated are left untouched, identical inputs always
produce identical output, and the original row order is restored on return.

When \code{limits} is supplied, the whole label group is rigidly shifted back
inside the panel if it crosses a boundary; a rigid shift preserves the
separations, so boundary handling never reintroduces collisions. If the
combined label widths cannot fit inside \code{limits}, the function emits a warning
and returns a best-effort, left-aligned layout with \code{feasible = FALSE} (see
\code{details}) rather than silently overlapping labels.

The helper targets Cartesian, continuous horizontal scales. Logarithmic,
transformed, or otherwise non-linear scales are not handled: convert the
positions to the linear coordinate system before calling.
}
\examples{
# Fixed-gap mode: four nearly identical values kept from overlapping
spread_labels_x(c(0.37, 0.38, 0.38, 0.38), min_gap = 0.03)

# Already-separated values are returned unchanged
spread_labels_x(c(0.1, 0.5, 0.9), min_gap = 0.03)

# The group is kept inside the panel limits
spread_labels_x(c(0.95, 0.96, 0.97), min_gap = 0.04, limits = c(0, 1))

# Diagnostics
spread_labels_x(c(0.5, 0.5, 0.5), min_gap = 0.05, details = TRUE)

# Text mode: separation sized from the rendered width of each label
vals <- c(0.37, 0.38, 0.38)
spread_labels_x(
  x      = vals,
  labels = scales::percent(vals, accuracy = 1),
  limits = c(0, 1)
)

\donttest{
# Typical use inside a plotting pipeline
library(dplyr)
library(ggplot2)
wjp_fonts()

chart_data <- tibble::tibble(
  outcome  = rep(c("Row A", "Row B"), each = 3),
  category = rep(c("G1", "G2", "G3"), times = 2),
  value    = c(0.37, 0.38, 0.38, 0.20, 0.55, 0.90)
)

label_data <- chart_data \%>\%
  group_by(outcome) \%>\%
  mutate(
    label   = scales::percent(value, accuracy = 1),
    label_x = spread_labels_x(value, labels = label, limits = c(0, 1))
  ) \%>\%
  ungroup()

ggplot(label_data, aes(value, outcome, color = category)) +
  geom_point(size = 3) +
  geom_text(aes(x = label_x, label = label), vjust = -0.8, show.legend = FALSE) +
  scale_x_continuous(limits = c(0, 1)) +
  WJP_theme()
}

}
