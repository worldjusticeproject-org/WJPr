% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/radarChart.R
\name{wjp_radar}
\alias{wjp_radar}
\title{Plot a Radar Chart following WJP style guidelines}
\usage{
wjp_radar(
  data,
  axis_var,
  target,
  labels,
  colors,
  maincat = NULL,
  cvec = NULL,
  order = NULL,
  source = "GPP",
  order_var = NULL,
  show_legend = FALSE
)
}
\arguments{
\item{data}{Data frame containing the data to plot.}

\item{axis_var}{String. Column name of the variable that supplies the axes
(dimensions) of the radar.}

\item{target}{String. Column name of the variable that supplies the values to plot.}

\item{labels}{String. Column name of the variable containing the axis labels to
display around the radar.}

\item{colors}{String. Column name of the variable that supplies the color grouping.
The plot shows one polygon per group.}

\item{maincat}{String. Column used to choose the axis labels. If \code{NULL}, labels are
taken from the first color group.}

\item{cvec}{Named vector of colors, one per group. Default is \code{NULL}
(the WJP contrast pair \verb{#482d8b} / \verb{#f26b21} is applied).}

\item{order}{String. Column name of the variable that contains the display order of
the axes. Default is \code{NULL} (data order).}

\item{source}{String. Either \code{"GPP"} (values on a 0-100 percentage scale) or
\code{"QRQ"} (values on a 0-1 score scale). Default is \code{"GPP"}.}

\item{order_var}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Use \code{order} instead.}

\item{show_legend}{Logical. If \code{TRUE}, displays a horizontal legend above the
chart using the color groups. Default is \code{FALSE}.}
}
\value{
A ggplot object representing the radar plot.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\code{wjp_radar()} takes a data frame with a specific data structure (usually long shaped) and returns a ggplot
object with a radar chart following WJP style guidelines.
}
\details{
The function expects long-format data with one row per \code{axis_var}
(dimension) and \code{colors} (group) combination; one polygon is drawn per
group. With \code{source = "GPP"} (default) values are read as percentages
(0-100) and the rings are labeled 0\%-100\%; with \code{source = "QRQ"} values
are read as scores (0-1). Axis labels are taken from the \code{labels} column
of the group given by \code{maincat} (or the first group when \code{maincat} is
\code{NULL}) and support HTML/markdown formatting when the ggtext package is
installed.
}
\examples{
library(dplyr)
library(tidyr)

# Always load the WJP fonts
wjp_fonts()

# Opinions about authorities, by gender
data4radar <- WJPr::gpp \%>\%
  select(gend, starts_with("q49")) \%>\%
  mutate(
    gend   = as.double(unclass(gend)),
    across(starts_with("q49"), \(x) as.double(unclass(x))),
    gender = case_when(gend == 1 ~ "Male", gend == 2 ~ "Female"),
    across(starts_with("q49"), \(x) case_when(x <= 2 ~ 1, x <= 99 ~ 0))
  ) \%>\%
  group_by(gender) \%>\%
  summarise(across(starts_with("q49"), \(x) mean(x, na.rm = TRUE) * 100)) \%>\%
  pivot_longer(!gender, names_to = "category", values_to = "percentage") \%>\%
  mutate(axis_label = category)

wjp_radar(
  data4radar,
  axis_var = "category",
  target   = "percentage",
  labels   = "axis_label",
  colors   = "gender",
  show_legend = TRUE,
  cvec     = c("Male" = "#482d8b", "Female" = "#f26b21")
)

# Rule of Law Index factor scores (0-1 scale) with source = "QRQ"
data4radar_roli <- WJPr::roli \%>\%
  filter(country == "Austria", year == 2024) \%>\%
  select(country, f1, f2, f3, f4, f5, f6, f7, f8) \%>\%
  pivot_longer(!country, names_to = "factor", values_to = "score") \%>\%
  mutate(factor_label = paste("Factor", substr(factor, 2, 2)))

wjp_radar(
  data4radar_roli,
  axis_var = "factor",
  target   = "score",
  labels   = "factor_label",
  colors   = "country",
  cvec     = c("Austria" = "#482d8b"),
  source   = "QRQ"
)

}
