% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lineChart.R
\name{wjp_lines}
\alias{wjp_lines}
\title{Plot a Line Chart following WJP style guidelines}
\usage{
wjp_lines(
  data,
  target,
  grouping,
  colors = NULL,
  cvec = NULL,
  labels = NULL,
  repel = FALSE,
  transparency = FALSE,
  transparencies = NULL,
  custom.axis = FALSE,
  x.breaks = NULL,
  x.labels = NULL,
  sec.ticks = NULL,
  ngroups = NULL,
  ptheme = WJP_theme()
)
}
\arguments{
\item{data}{Data frame containing the data to plot.}

\item{target}{String. Column name of the variable that supplies the values to plot.}

\item{grouping}{String. Column name of the variable that supplies the X-axis values
(usually years).}

\item{colors}{String. Column name of the variable that defines the lines and their
color grouping. Default is \code{NULL} (a single line is drawn).}

\item{cvec}{Named vector of colors, one per line. Names should match the values of
the \code{colors} variable. Default is \code{NULL} (the WJP palette, see \code{\link[=wjp_palette]{wjp_palette()}},
is applied).}

\item{labels}{String. Column name of the variable containing the value labels to
display. Default is \code{NULL} (no labels).}

\item{repel}{Logical. If \code{TRUE}, the ggrepel package is used to avoid overlapping
labels. Default is \code{FALSE}.}

\item{transparency}{Logical. If \code{TRUE}, per-line opacities given in \code{transparencies}
are applied. Default is \code{FALSE}.}

\item{transparencies}{Named vector of opacities, one per line. Required when
\code{transparency = TRUE}.}

\item{custom.axis}{Logical. If \code{TRUE}, \code{x.breaks} and \code{x.labels} are applied to a
continuous X-axis (requires the ggh4x package). Default is \code{FALSE}.}

\item{x.breaks}{Numeric vector with custom breaks for the X-axis.}

\item{x.labels}{Character vector with labels for the X-axis. Must have the same
length as \code{x.breaks}.}

\item{sec.ticks}{Numeric vector with minor breaks for the X-axis.}

\item{ngroups}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Grouping vector for the lines.
Retained for backwards compatibility; lines are now grouped by \code{colors}
automatically.}

\item{ptheme}{ggplot theme to apply. Default is \code{\link[=WJP_theme]{WJP_theme()}}.}
}
\value{
A ggplot object.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\code{wjp_lines()} takes a data frame with long-format data and returns a ggplot
object with a line chart following WJP style guidelines. Each line is defined
by the \code{colors} variable, so the function works out of the box for both single
and multiple series. Values are expected on a 0-100 percentage scale.
}
\examples{
library(dplyr)
library(tidyr)

# Always load the WJP fonts
wjp_fonts()

# Percentage of people that trust their institutions, over time
data4lines <- WJPr::gpp \%>\%
  filter(country == "Atlantis") \%>\%
  select(year, q1a, q1b, q1c) \%>\%
  mutate(
    across(!year, \(x) as.double(unclass(x))),
    across(!year, ~ case_when(.x <= 2 ~ 1, .x <= 4 ~ 0)),
    year = as.character(year)
  ) \%>\%
  group_by(year) \%>\%
  summarise(across(everything(), \(x) mean(x, na.rm = TRUE) * 100)) \%>\%
  pivot_longer(!year, names_to = "variable", values_to = "percentage") \%>\%
  mutate(
    institution = case_when(
      variable == "q1a" ~ "Institution A",
      variable == "q1b" ~ "Institution B",
      variable == "q1c" ~ "Institution C"
    ),
    value_label = paste0(round(percentage, 0), "\%")
  )

# Multiple lines, one per institution
wjp_lines(
  data4lines,
  target   = "percentage",
  grouping = "year",
  colors   = "institution",
  labels   = "value_label",
  repel    = TRUE,
  cvec     = c("Institution A" = "#482d8b",
               "Institution B" = "#2894aa",
               "Institution C" = "#f26b21")
)

# Single line
wjp_lines(
  data4lines \%>\% filter(institution == "Institution A"),
  target   = "percentage",
  grouping = "year",
  colors   = "institution",
  labels   = "value_label"
)

}
