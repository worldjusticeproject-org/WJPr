% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gaugeChart.R
\name{wjp_gauge}
\alias{wjp_gauge}
\title{Plot a Gauge Chart following WJP style guidelines}
\usage{
wjp_gauge(
  data,
  target,
  colors,
  cvec = NULL,
  factor_order = NULL,
  labels = NULL,
  crop = c(-10, 0, 0, -8),
  ptheme = WJP_theme(),
  show_legend = FALSE
)
}
\arguments{
\item{data}{Data frame containing the data to plot.}

\item{target}{String. Column name of the variable that supplies the values to plot.}

\item{colors}{String. Column name of the variable that supplies the color grouping
for the segments.}

\item{cvec}{Named vector of colors, one per segment. Names should match the values
of the \code{colors} variable. Default is \code{NULL} (the WJP palette, see
\code{\link[=wjp_palette]{wjp_palette()}}, is applied).}

\item{factor_order}{Vector with the order in which the segments should be plotted.
Default is \code{NULL} (data order).}

\item{labels}{String. Column name of the variable containing the labels to display
inside the segments. Default is \code{NULL} (no labels).}

\item{crop}{Numeric vector with the space to crop from the Top, Right, Bottom, and
Left margins, respectively. Default is \code{c(-10, 0, 0, -8)}.}

\item{ptheme}{ggplot theme to apply. Default is \code{\link[=WJP_theme]{WJP_theme()}}.}

\item{show_legend}{Logical. If \code{TRUE}, displays a horizontal legend above the
chart using the segment categories. Default is \code{FALSE}.}
}
\value{
A ggplot object representing the gauge chart.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\code{wjp_gauge()} creates a gauge (speedometer) chart using ggplot2 based on the provided data frame.
The chart displays segments in a semicircle, useful for showing composition or progress.
}
\details{
The function expects one row per segment: a category in \code{colors}, its value
in \code{target}, and an optional display text in \code{labels}. Values are rescaled
so the segments span the semicircle, and labels are hidden automatically
for segments that are too small to hold them (below 5\% of the total).
Use \code{factor_order} to control the drawing order of the segments.
}
\examples{
library(dplyr)
library(ggplot2)

# Always load the WJP fonts (optional)
wjp_fonts()

# Create sample data for gauge chart
data4gauge <- data.frame(
  category = c("Category A", "Category B", "Category C", "Category D"),
  value = c(25, 35, 20, 20),
  label = c("25\%", "35\%", "20\%", "20\%")
)

# Define colors for each segment
gauge_colors <- c(
  "Category A" = "#482d8b",
  "Category B" = "#2894aa",
  "Category C" = "#f26b21",
  "Category D" = "#555659"
)

# Plotting chart
wjp_gauge(
  data4gauge,
  target       = "value",
  colors       = "category",
  cvec         = gauge_colors,
  factor_order = c("Category A", "Category B", "Category C", "Category D"),
  labels       = "label",
  show_legend  = TRUE
)

# Minimal call: segments default to the WJP palette
wjp_gauge(
  data4gauge,
  target = "value",
  colors = "category",
  labels = "label"
)

}
